import * as path from 'path';

export default class FileManager {
    // METHODS /////////////////////////////////////////////////////////////
    public GetFiles(path: string, recursive: boolean): any[] {
        return [];
    }
}